allow audioserver qmuxd_socket:dir { write remove_name  add_name };
allow audioserver qmuxd_socket:sock_file { write create setattr unlink };
allow audioserver audioserver:socket { create ioctl };
allow audioserver qmuxd:unix_stream_socket { connectto };
